1. 12*10 + 12*20 + 4*10
2. 12 레퍼런스를 넘기니까 값이 변환됨
3. 11   f2 무시
f3 num을 가리키는 참조자를 반환한 것에 3을 더해서 11
f4 지역변수 레퍼런스에 4를더한것이라 무시
4. 기본생성자, 3입력!   첫번째는 함수선언
5. 멤버 변수를 외부에서 직접 사용하지않고 멤버 함수를 통해 사용하도록 하는 것, 멤버 변수를 메소드로 감싸는 것
클래스 내부 값 변경 시 외부에서 하나하나 써야함, 클래스가 어떻게 작동하는지 구체적으로 알필요가 있음